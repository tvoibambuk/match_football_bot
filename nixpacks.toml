[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python bot.py"

[phases.build]
cmds = ["pip install -r requirements.txt"]

[environment]
PYTHON_VERSION = "3.11"